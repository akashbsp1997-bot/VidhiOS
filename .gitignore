# dependencies
/node_modules

# next.js
/.next/
/out/

# env files (never commit real secrets)
.env
.env.local
.env.*.local

# drizzle/ migration SQL files are committed (Phase 1 switched from db:push to
# versioned db:generate + db:migrate) -- do NOT ignore them.

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store

# editor
.vscode/
.idea/
